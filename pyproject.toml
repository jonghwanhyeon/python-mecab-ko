[build-system]
requires = [
    "setuptools>=42",
    "pybind11>=2.10.0",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
# Skip building on CPython 3.6 on all platforms
skip = "cp36-*"